{
  "name": "slimy-monorepo",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "lint": "pnpm -r run lint || echo \"TODO: lint\"",
    "build": "pnpm -r run build",
    "test": "pnpm -r run test || echo \"TODO: wire tests\""
  }
}
