# Web app env template for self-hosted slimy.ai

NODE_ENV=production
PORT=3000

# Base URL for Admin API (per-host override)
# Example: http://192.168.68.64:3080
NEXT_PUBLIC_API_URL=http://CHANGE_ME:3080
NEXT_PUBLIC_ADMIN_API_BASE=http://CHANGE_ME:3080
NEXT_PUBLIC_DISCORD_CLIENT_ID=

# Optional: Codes aggregator / Snelp integration
# NEXT_PUBLIC_SNELP_CODES_URL=https://snelp.com/codes/

# Trader UI (trader.slimyai.xyz)
# NEXT_PUBLIC_TRADER_API_BASE=http://127.0.0.1:8080  # Empty = mock mode
# NEXT_PUBLIC_TRADER_ADAPTER=mock                     # mock | http
# NEXT_PUBLIC_BUILD_VERSION=dev

# Trader Artifacts (Phase 6) - Private read-only viewer
# Comma-separated list of TraderUser IDs allowed to view artifacts
# Leave empty to deny all access (fail-closed)
# TRADER_ARTIFACTS_ALLOWED_USER_IDS=clxxxxxxxxx,clyyyyyyyyyy

# Path to local artifacts (pulled from NUC1 by systemd timer)
# TRADER_ARTIFACTS_PATH=/var/lib/trader-artifacts/shadow
# TRADER_ARTIFACTS_STATUS_FILE=/var/lib/trader-artifacts/status.json
# TRADER_ARTIFACTS_STALE_THRESHOLD_SEC=300
