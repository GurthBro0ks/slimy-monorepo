# Test Environment Configuration
# Copy this file to `.env.test.local` (gitignored) for local test overrides.
# NEVER use production credentials or a production database in tests.

# Database connection (optional)
# Most tests mock Prisma and do NOT need a real database.
# If you do set this, use a dedicated *_test database.
DATABASE_URL="mysql://slimy_test:CHANGE_ME@127.0.0.1:3306/slimyai_test"

# Admin API endpoints (already set in tests/setup.ts, but can override here)
# NEXT_PUBLIC_ADMIN_API_BASE="https://admin.example.test"
# NEXT_PUBLIC_SNELP_CODES_URL="https://snelp.example.test/api/codes"
