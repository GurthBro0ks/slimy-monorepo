# TRUTH PASS — slimy-monorepo — 2026-03-23T04:50:00Z

## EXECUTIVE SUMMARY

**STATUS: DEGRADED**
The web app IS running and serving healthy responses on port 3000, but PM2 process management is broken (30 restart crashes) and lint fails with 89 errors blocking clean commits.

---

## REPO PATH
`/opt/slimy/slimy-monorepo` (symlink: `/home/slimy/slimy-monorepo`)

## BRANCH / DIRTY
- Branch: `feature/merge-chat-app`
- Dirty: **CLEAN** (no uncommitted changes)
- Up to date with origin/feature/merge-chat-app

## BUILD/LINT/TYPECHECK STATUS

| Check | Result | Details |
|-------|--------|---------|
| `pnpm install --frozen-lockfile` | ✅ PASS | Dependencies installed, lockfile healthy |
| `pnpm lint` | ❌ FAIL | **89 errors, 41 warnings** |
| `pnpm build:web` | ✅ PASS | Post-build validation passes, bundle sizes OK |
| `pnpm -w typecheck` | NOT RUN | No workspace-level typecheck defined |

### Lint Error Breakdown (89 errors)
Core patterns across multiple files:
1. **`no-unused-vars`** (~50 errors): `Suspense`, `MCPToolCall`, `onMessage`, `alert`, `config`, `existsSync`, `filePath`, `data`, `presence`, `Image`, `Zap`, `Card*`, `Callout`, `MOCK_GUILD_ID`, `parseToken`, `thirtyDaysAgo`, `totalCompletions`, `ctx`, `AirdropCalendar`, `Badge`, `LogLine`, etc.
2. **`react/no-unescaped-entities`** (~20 errors): Unescaped `"` and `'` in JSX across `chat/page.tsx`, `club/page.tsx`, and other pages
3. **`deprecation/deprecation`** (~41 warnings): `z.url()`, `z.iso.datetime()`, `z.merge()`, `String.substr()`

Errors are in these files (not exhaustive):
- `apps/web/lib/lazy.tsx` — `Suspense` unused
- `apps/web/lib/mcp-client.ts` — `MCPToolCall`, `onMessage` unused
- `apps/web/lib/monitoring/alerting.ts` — `alert` unused param
- `apps/web/lib/security/ddos-protection.ts` — `config` unused
- `apps/web/scripts/import-docs.ts` — `existsSync`, `filePath` unused
- `apps/web/app/chat/page.tsx` — multiple unused imports + unescaped entities
- `apps/web/app/club/page.tsx` — multiple unused imports + unescaped entities

---

## RUNTIME STATUS

| Service | Status | Port | Details |
|---------|--------|------|---------|
| slimyai-web (standalone) | ✅ RUNNING | 3000 | `next-server (v16.0.7)` PID 2007305 |
| slimyai-web (PM2) | ❌ ERRORED | — | 30 restarts, EADDRINUSE on 3000 |
| MySQL | ✅ RUNNING | 3306 | Docker |
| mission-control | ✅ RUNNING | 3838 | |

### Runtime Details
- **Actual server**: `next-server (v16.0.7)` running on port 3000 (PID 2007305, started Mar 22 via shell snapshot)
- **Server health**: `curl localhost:3000/api/codes/health` → `{"status":"healthy","service":"codes"}`
- **curl tests**: `/` → 200, `/owner/crypto` → 307 (auth redirect, correct)
- **PM2 crash reason**: Tries to start `pnpm start` in `apps/web` cwd, but port 3000 is already bound

---

## TOP 3 REAL PROBLEMS

### 1. PM2 `slimyai-web` is in Crash Loop — Port 3000 Conflict (CONFIDENCE: 1.0)
**Problem**: PM2 process `slimyai-web` is errored after 30 consecutive restart failures. The orphan `next-server` process (PID 2007305, Mar 22) already occupies port 3000. PM2 keeps trying to bind the same port and failing.

**Evidence**:
```
Error: listen EADDRINUSE: address already in use 0.0.0.0:3000
⨯ Failed to start server
```

**Root cause**: Server was started manually via a shell snapshot script on Mar 22 and is now orphaned (running but not managed by PM2). PM2 ecosystem.config.js still has `web` entry trying to bind the same port.

**Fix options**:
- **Option A** (quick): `pm2 delete slimyai-web` — stops PM2 from crashing, leaves orphan server running
- **Option B** (proper): `kill 2007305` then `pm2 restart slimyai-web` — reparent to PM2
- **Note**: `ecosystem.config.js` also still lists `admin-ui` and `admin-api` entries (dead services) — cleanup needed but not blocking

### 2. Lint Fails with 89 Errors — Blocks Clean Commits (CONFIDENCE: 1.0)
**Problem**: `pnpm lint` returns exit code 1 with 89 errors. Pre-commit hooks would block commits. Errors span multiple files in `apps/web/` including chat page, club page, lib utilities, and monitoring code.

**Evidence**: `pnpm lint 2>&1 | grep "✖"` → `✖ 130 problems (89 errors, 41 warnings)`

**Root cause**: New unused variables and imports introduced in recent commits (SS3/SS4 chat-app integration, club page) that were committed with `--no-verify` due to pre-existing lint issues.

**Fix**: Rename unused vars to `_name` pattern OR remove unused imports/declarations across ~10 files in `apps/web/`. This is the single highest-value fix to unblock clean git workflow.

### 3. ecosystem.config.js Lists Dead Services (CONFIDENCE: 1.0)
**Problem**: `ecosystem.config.js` still defines `admin-api` (port 3080) and `admin-ui` (port 3081) entries, both intentionally dead since 2026-03-19 per AGENTS.md.

**Evidence**: `cat ecosystem.config.js` shows all three apps listed.

**Fix**: Remove dead entries from ecosystem.config.js. Minor but contributes to confusion about intended state.

---

## RECOMMENDED NEXT FIX

**TOP 1 FIX**: Fix lint errors to enable clean commits

Fix the 89 lint errors across these files in `apps/web/`:

| File | Error Count | Error Type |
|------|-------------|-------------|
| `lib/lazy.tsx` | 1 | unused Suspense |
| `lib/mcp-client.ts` | 2 | unused MCPToolCall, onMessage |
| `lib/monitoring/alerting.ts` | 1 | unused alert param |
| `lib/security/ddos-protection.ts` | 1 | unused config |
| `scripts/import-docs.ts` | 2 | unused existsSync, filePath |
| `app/chat/page.tsx` | ~20 | unused imports + unescaped entities |
| `app/club/page.tsx` | ~15 | unused imports + unescaped entities |
| Other files | ~47 | various unused vars |

**Secondary fix**: `pm2 delete slimyai-web` to stop crash loop noise.

---

## COMMANDS RUN

```bash
cd /opt/slimy/slimy-monorepo
git status                           # CLEAN
git branch                          # feature/merge-chat-app
git log --oneline -15               # HEAD: 09cbdbb (TRUTH_PASS doc commit)
node --version && pnpm --version    # v22.22.0 / pnpm 10.22.0
pnpm lint                           # 89 errors, 41 warnings (FAILS)
pnpm build:web                      # PASS (postbuild validation OK)
pm2 list                            # slimyai-web: errored, 30 restarts
ss -tlnp | grep 3000               # next-server (v16.0.7) PID 2007305
curl localhost:3000/                 # HTTP 200
curl localhost:3000/owner/crypto    # HTTP 307 (auth redirect, correct)
curl localhost:3000/api/codes/health # {"status":"healthy","service":"codes"}
ps aux | grep next                  # PID 2007305: next-server (v16.0.7)
```

---

## STALE ASSUMPTIONS IDENTIFIED

1. **AGENTS.md line "Live code: branch: feature/merge-chat-app"** — Still accurate, branch is current.

2. **Feature list note "lint-cleanup-001 passes"** — Still true for CryptoDashboard.tsx specifically, but lint overall still FAILS due to errors in OTHER files. The lint-cleanup-001 fixed its target file but new errors were introduced by subsequent commits.

3. **PM2 ecosystem.config.js `name: "web"`** — PM2 actually registers this as `slimyai-web` (name mismatch). The config says `name: "web"` but PM2 shows `slimyai-web`. This is confusing.

4. **`/api/* → 3080` rewrite is dead** — Correctly documented in AGENTS.md as of 2026-03-21. No stale rewrite catching traffic.

5. **`admin-api.archived-20260319/`** — Present in apps/, correctly labeled as archived. Not interfering.

---

## CONFIDENCE SUMMARY

| Finding | Confidence |
|---------|------------|
| Site is live and serving HTTP 200 on port 3000 | 1.0 |
| API health endpoint returns healthy | 1.0 |
| PM2 slimyai-web is in crash loop | 1.0 |
| Root cause is port 3000 EADDRINUSE | 1.0 |
| 89 lint errors block clean commits | 1.0 |
| Ecosystem.config.js lists dead services | 1.0 |
| Git working tree is clean | 1.0 |
| Build passes | 0.95 |
| site is actually serving traffic via standalone (not PM2-managed) | 1.0 |
