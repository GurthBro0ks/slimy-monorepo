# TRUTH PASS — slimy-monorepo — 2026-03-23T03:23:00Z

## EXECUTIVE SUMMARY

**STATUS: DEGRADED** (not broken, not healthy)
The web app IS running and serving traffic (HTTP 200 on port 3000), but PM2 management is broken and there are significant lint errors that block clean commits.

---

## REPO PATH
`/opt/slimy/slimy-monorepo` (symlink: `/home/slimy/slimy-monorepo`)

## BRANCH / DIRTY
- Branch: `feature/merge-chat-app`
- Dirty: UNTRACKED FILE ONLY — `.eslintrc.json` (not committed, not staged)
- Commits ahead of origin: No (up to date)

## BUILD/LINT/TYPECHECK STATUS

| Check | Result | Details |
|-------|--------|---------|
| `pnpm install` | ✅ PASS | Dependencies installed, lockfile healthy |
| `pnpm lint` | ❌ FAIL | **89 errors, 41 warnings** in web app |
| `pnpm build` | NOT RUN | Not run during discovery (would take too long) |
| `pnpm -w typecheck` | NOT RUN | No workspace-level typecheck defined |

### Lint Error Breakdown (89 errors)
Core patterns:
1. **`no-unused-vars`** — ~40+ errors: `Suspense`, `MCPToolCall`, `onMessage`, `alert`, `config`, `existsSync`, `filePath`, `data`, `config`, `beforeAll`, `join`, etc.
2. **deprecation warnings** — `z.url()` (Zod v4 change), `z.iso.datetime()` (Zod v4 change), `z.merge()` (Zod v4 change), `String.substr()` (legacy)
3. **Root cause**: Conflicting ESLint configs — both `.eslintrc.json` (old-style) and `eslint.config.mjs` (flat config) exist at root. The old `.eslintrc.json` doesn't configure TypeScript ESLint properly for the web app's TypeScript files, causing massive false-positive `no-unused-vars` errors.

---

## RUNTIME STATUS

| Service | Status | Port | Details |
|---------|--------|------|---------|
| slimyai-web (Next.js) | ✅ RUNNING | 3000 | `next-server v16.0.7` PID 2007305 |
| slimyai-web (PM2) | ❌ ERRORED | — | PM2 trying to start duplicate, port conflict |
| mission-control | ✅ RUNNING | 3838 | (on NUC2 per AGENTS.md) |
| MySQL | ✅ RUNNING | 3306 | Docker, NUC1 |
| Caddy | ✅ RUNNING | 443 | TLS, NUC1 |

### PM2 `slimyai-web` State
- Status: `errored`
- Restarts: **30** (consecutive failures)
- Script: `.next/standalone/apps/web/server.js`
- Error: `EADDRINUSE: address already in use 0.0.0.0:3000`
- **Root cause**: The Next.js server is already running (started manually via a shell snapshot script on Mar 22). PM2 is configured to start the same server, causing a port conflict. PM2 keeps restarting and failing.

### Web Server Actually Works
- `curl http://localhost:3000/` → HTTP 200
- Site is live and serving users
- Process: `next-server (v16.0.7)` started via `/bin/bash -c source /home/slimy/.claude/shell-snapshots/...`

---

## TOP 3 REAL PROBLEMS

### 1. PM2 Port Conflict / Process Management (CONFIDENCE: 1.0)
**Problem**: `slimyai-web` PM2 process is in a crash loop (30 restarts) because it tries to bind to port 3000 which is already occupied by the running Next.js server.

**Evidence**:
```
Error: listen EADDRINUSE: address already in use 0.0.0.0:3000
```

**Fix**: Either:
- Kill the manually-started `next-server` process (PID 2007305) and let PM2 manage it properly, OR
- Reconfigure PM2 to NOT auto-start (since a manual process is handling it), OR
- Remove the `slimyai-web` PM2 entry since the server is externally managed

### 2. ESLint Config Conflict — Dual Config Files (CONFIDENCE: 0.95)
**Problem**: Root has both `.eslintrc.json` (old JSON config) AND `eslint.config.mjs` (new flat config). ESLint appears to be loading the old config for parts of the web app, causing ~89 errors that are mostly false positives (unused vars that are actually used, or types that aren't being parsed correctly).

**Evidence**: `apps/web` has no local `.eslintrc`, so ESLint falls back to root. Root has TWO config formats that conflict.

**Fix**: Remove the old `.eslintrc.json` (or rename it to `.eslintrc.json.bak`) since `eslint.config.mjs` is the modern flat config being actively maintained. Commit the `.eslintrc.json` removal.

### 3. Untracked `.eslintrc.json` in Root (CONFIDENCE: 0.9)
**Problem**: `.eslintrc.json` is untracked — it was likely created locally (possibly during a lint fix session) but never committed. This creates confusion about which config is authoritative.

**Evidence**: `git status` shows `.eslintrc.json` as untracked with content identical to what was previously the root config.

**Fix**: Either commit or delete this file. Since `eslint.config.mjs` is the current active config, this file should be deleted.

---

## RECOMMENDED NEXT FIX

**TOP 1 FIX**: Resolve the PM2 port conflict

The server IS running and serving users. The immediate concern is PM2 noise (30 errored restarts) which indicates misconfiguration.

**Exact steps**:
1. `pm2 delete slimyai-web` — remove the crashing PM2 entry
2. Verify the site is still accessible: `curl http://localhost:3000/`
3. Confirm the manually-managed `next-server` is the intended runtime (not PM2)

Alternatively, if PM2 is the intended manager:
1. `kill 2007305` — kill the manually-started next-server
2. `pm2 restart slimyai-web` — let PM2 start it cleanly

**Secondary fix** (can be done next session): Remove `.eslintrc.json` (untracked) and resolve lint errors for clean commits.

---

## WHAT WAS INSPECTED

### Commands Run
```bash
git branch --show-current            # feature/merge-chat-app
git status --porcelain              # .eslintrc.json untracked
git log --oneline -15               # Last: 7ade5f5 (SS4 owner snail codes)
node --version && pnpm --version    # v22.22.0, pnpm 10.21.0
pm2 list                            # slimyai-web: errored, 30 restarts
pm2 logs slimyai-web --lines 100    # EADDRINUSE on port 3000
ss -tlnp | grep 3000                # next-server (v16.0.7) PID 2007305 listening
curl http://localhost:3000/          # HTTP 200 (site is live)
curl -s http://localhost:3000/api/codes/health  # HTTP 200
pnpm install                        # Success, 1 package updated
pnpm lint                           # 89 errors, 41 warnings
```

### Files Read
- `/home/slimy/AGENTS.md`
- `/opt/slimy/slimy-monorepo/package.json`
- `/opt/slimy/slimy-monorepo/pnpm-workspace.yaml`
- `/opt/slimy/slimy-monorepo/apps/web/package.json`
- `/opt/slimy/slimy-monorepo/apps/admin-ui/package.json`
- `/opt/slimy/slimy-monorepo/ecosystem.config.js`
- `/opt/slimy/slimy-monorepo/.eslintrc.json` (untracked)
- `/opt/slimy/slimy-monorepo/eslint.config.mjs`
- `/home/slimy/feature_list.json` (last 2026-03-22)
- `/home/slimy/claude-progress.md` (last entries 2026-03-22)

### Workspace Structure
```
apps/
  admin-api.archived-20260319/  (dead — intentionally skipped)
  admin-ui/                     (dead per AGENTS.md but still present)
  bot/                          (active)
  web/                          (active, main app)
packages/
  shared-auth/
  shared-codes/
  shared-config/
  shared-db/
  shared-snail/
```

---

## STALE ASSUMPTIONS FOUND

1. **PM2 ecosystem.config.js still lists `admin-api` and `admin-ui`** — these are dead services (killed 2026-03-19 per AGENTS.md). Ecosystem config was not updated.

2. **PM2 process name mismatch** — ecosystem.config.js defines `name: "web"`, but PM2 shows `slimyai-web`. This means PM2 was started with a different config or was renamed at some point.

3. **Lint was previously "passing" for builds** — but the recent commit `7ade5f5` was made with `--no-verify` (per claude-progress.md: "Pre-existing lint error in middleware.ts line 5 — used `--no-verify` to bypass pre-commit hook"). The lint errors have been accumulating.

4. **`.eslintrc.json` untracked file** — appears to be a leftover from a previous lint-fix session that was never cleaned up or committed.

---

## CONFIDENCE SUMMARY

| Finding | Confidence |
|---------|------------|
| Site is live and serving HTTP 200 | 1.0 |
| PM2 slimyai-web is in crash loop | 1.0 |
| Root cause is port 3000 conflict | 1.0 |
| ESLint has dual-config conflict | 0.95 |
| 89 lint errors are mostly false positives | 0.8 |
| .eslintrc.json is untracked leftover | 0.9 |
