# Docs — Agent Rules

## Facts first
- Keep `CONTINUITY.md` short, stable, and facts-only.
- Put “how-to” and long timelines in `docs/` instead of the continuity ledger.

## Buglog
- Use `docs/buglog/` as the flight recorder: commands, outputs, screenshots/snippets as needed.

