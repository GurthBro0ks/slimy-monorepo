# Scripts — Agent Rules

## Determinism
- Prefer scripts that are deterministic and safe to run repeatedly.
- Fail fast with clear errors and non-zero exit codes.

## Evidence
- For non-trivial changes, capture script output in `docs/buglog/`.

