# Git Dirt Watcher - Environment Configuration
#
# Copy this file to .env and configure your settings:
#   cp .env.example .env
#   nano .env
#
# Then source it before running the script:
#   source .env
#   ./git-dirt-watch.sh

# ==============================================================================
# REQUIRED CONFIGURATION
# ==============================================================================

# Discord webhook URL for notifications
# Create a webhook in Discord: Server Settings > Integrations > Webhooks > New Webhook
# Example: https://discord.com/api/webhooks/123456789/abcdefghijklmnopqrstuvwxyz
DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/YOUR_WEBHOOK_ID/YOUR_WEBHOOK_TOKEN

# ==============================================================================
# OPTIONAL CONFIGURATION
# ==============================================================================

# Maximum number of filenames to include in the notification (default: 10)
# This prevents the Discord message from becoming too large when many files are changed
DIRT_MAX_FILES=10

# Dry run mode - set to 1 to print payload to stdout instead of sending to Discord
# Useful for testing and debugging
# DRY_RUN=0

# ==============================================================================
# USAGE EXAMPLES
# ==============================================================================
#
# Test without sending to Discord:
#   DRY_RUN=1 ./git-dirt-watch.sh
#
# Run with custom repos file:
#   ./git-dirt-watch.sh /opt/slimy/config/production-repos.txt
#
# Run from systemd timer (source .env in systemd service unit):
#   EnvironmentFile=/opt/slimy/tools/git-dirt-watch/.env
#
# ==============================================================================
# SECURITY NOTES
# ==============================================================================
#
# - This file contains sensitive webhook URLs
# - Never commit .env to version control (use .gitignore)
# - Only commit .env.example with placeholder values
# - Restrict file permissions: chmod 600 .env
# - Use separate webhooks for different environments (dev/staging/prod)
