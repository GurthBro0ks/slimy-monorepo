# Auto-Deploy Environment Configuration
#
# Copy this file to .env and customize for your environment
# cp .env.example .env

# ============================================================================
# Deployment Configuration
# ============================================================================

# Path to the deployment configuration YAML file
# Default: ./deploy-host-config.yml
# DEPLOY_CONFIG=/opt/slimy/scripts/auto-deploy/deploy-host-config.yml

# ============================================================================
# Test & Build Options
# ============================================================================

# Skip test execution during deployment (use with caution!)
# Set to 1 to skip tests, 0 to run tests (default)
# WARNING: Skipping tests in production is not recommended
SKIP_TESTS=0

# ============================================================================
# Notification Configuration
# ============================================================================

# Discord webhook URL for deployment notifications
# Leave empty to disable Discord notifications
# To create a webhook: Server Settings > Integrations > Webhooks > New Webhook
# DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/YOUR_WEBHOOK_ID/YOUR_WEBHOOK_TOKEN

# ============================================================================
# Advanced Options
# ============================================================================

# Git timeout (in seconds) for fetch/pull operations
# GIT_TIMEOUT=300

# Maximum number of deployment retries on failure
# MAX_RETRIES=0

# Deployment lock file to prevent concurrent runs
# LOCK_FILE=/tmp/auto-deploy.lock
