{
  "name": "@slimy/opps-runtime",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Experimental opportunities runtime for offer scoring and coverage",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "node --test tests/**/*.test.ts",
    "test:watch": "node --test --watch tests/**/*.test.ts"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  }
}
